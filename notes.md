# Notes

The algo runs and saves the gradients. There is a problem in reading them by the divider. We assumes that this is due to some sort of corruption that happened during the sending process.

Remember that to run the code you should run `python3 ./provisioner.py `, `python3 worker.py`, and `python3 coordinator.py ` first.

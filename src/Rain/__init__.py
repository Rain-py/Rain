# from Rain.Worker.WorkerAmbassador import WorkerAmbassador
# BASE_PORT = 50151

def worker_serve():
    """Entry point for the application script"""
    print("We will make the worker start serving here!")
    # worker = WorkerAmbassador(BASE_PORT)
    # worker.serve()